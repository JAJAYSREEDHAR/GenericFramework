package com.company.selenium.api;

/**
 * Using this Enum appropriate browser can be invoked <br>
 * <b>Syntax: </b> locator =LocatorType.id;
 * 
 * @author Ajay
 *
 */
public enum LocatorType {
	id, className, name, link, partialLink, tagname, xpath, cssSelect, below, above, toLeftOf, toRightOf, nearByElement,
	nearByPixel
}
